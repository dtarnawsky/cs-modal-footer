import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.modal.footer',
  appName: 'cs-modal-footer',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
